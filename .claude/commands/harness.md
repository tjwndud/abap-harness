Use the ABAP harness framework for this task.

Workflow:

1. Read `/CLAUDE.md` first.
2. Read `/docs/INDEX.md` only to choose the smallest extra reference.
3. Inspect the target repository for the closest existing ABAP example before implementing.
4. If the task is BDC, CDS, AMDP, or Gateway, load only the matching file under `/docs/rules/`.
5. Follow repository style before introducing a cleaner new pattern.
6. Before finishing, check the result against the loaded harness rules.

Do not load every rule file by default.
